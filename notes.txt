random:

virtualenv push/pop

for coda:

markdown preview window
select line like textmate
python clips
startup commands for terminal
env vars
ignore files
terminal home/end
export/import sites
alias coda='open -a /Applications/Coda.app'
todo pane (from things?)
install cmd+` shortcut for ``select next tab``
adds background option for remote shell?
Highlight TODO's

for django:

pavement.py:
    import django.settings for installed apps with skel? won't work for core?

paver bootstrap script:
    skel/bin/skel-admin.py
        startproject <projectname> --venv [path, default=./<projectname>/.virtualenv] --dev [default off]
            creates ./<projectname>/
                        local_settings.py
                        settings.py
                        settings_dev.py
                        manage.py (with new venv path to activate)
                        urls.py
                        templates/
                            base.html
                            navigation.html
                        static/
                            css/
                                960.css
                                base.css
                                reset.css
                                text.css
                            img/
                                12_col_vertical.gif
                                12_col.gif
                                16_col.gif
                            js/
                        requirements.txt
                        svn-ignores file
            exec pip install -E <venv dir> -r <requirements.txt>
            

paver setup.py script:
    parse requirements.txt into SKEL_REQS
    call paver.virtual.bootstrap(script_name='skel/bin/skel-venv.py', packages_to_install=SKEL_REQS)
    add packages_to_install options to paver.virtual.bootstrap() from requirements.txt
    links skel-admin.py into python /bin folder
    

#requirements    
-e svn+http://code.djangoproject.com/svn/django/trunk#egg=Django
-e svn+http://django-tagging.googlecode.com/svn/trunk#egg=Tagging
-e svn+http://django-template-utils.googlecode.com/svn/trunk#egg=Template_utils
